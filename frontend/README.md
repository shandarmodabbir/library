# Library frontend

See [the project README](../README.md) for complete setup and configuration.

With the backend running on port 8000:

```sh
npm ci
npm run dev -- --host 127.0.0.1
```

Open http://127.0.0.1:5173. `npm run build` creates `dist/`.
